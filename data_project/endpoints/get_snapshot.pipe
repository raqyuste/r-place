
TOKEN "pixels_table_write" READ

DESCRIPTION >
	This endpoints get the version filtered by timestamp

NODE endpoint
SQL >

    %
    SELECT x
          ,y
          ,argMax(color, timestamp)
      FROM pixels_table
    {% if defined(date_start) %}
      where  timestamp >= toDateTime({{DateTime(date_start)}})
    {% end %}
    group by x,y


